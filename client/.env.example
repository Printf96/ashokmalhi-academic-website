# Copy to .env.local for local overrides.
# Leave blank in development to use the Vite dev proxy to the local API
# (see vite.config.ts). Set to the production API origin only if the
# client and API are deployed to different hosts.
VITE_API_BASE_URL=
